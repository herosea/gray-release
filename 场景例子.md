## 一些场景介绍

### 常规技巧

#### 尽可能频繁的提交代码

这个不是提示词，是一种习惯。可以手动，可以自动（比如在 AGENTS.md里面加自动提交的规则）

当然，也可以提示词要codex干

```
提交代码
```

可以做成斜杆命令，会更快一点，提示词也能写更多一点。

#### 让Agent 反问

```
xxx, 有任何不清楚的，请提出来
```

#### 多专家角度思考

```
请从相关领域专家的角度审查本项目（xx文档）
```

````
对于xxx 请从各专业角度提出意见
````

### 项目了解

#### 帮我分析项目，输出架构设计文档



#### 找配置方案

```
superset 报表加载太慢了，有没有办法加载快一点
```

```
superset 的缓存
```

```
我想要xxx，现在的情况是xxx，现在的配置有xxx，有什么方式实现我的需求
```



### 需求文档的操作

#### 拆分文档（拆小一点，好分开设计实现）



#### 抽取特定功能的文档（类似excel的透视）

需求文档有时候会比较分散，通过特定角度抽取，重新组织文档到另外一个文档（可以是临时的）

#### 设计文档到需求文档的反向同步

可以在根据需求文档生成设计文档的时候，如果讨论的差不多了，该确认的都确认了，给ai发一句（建议执行之前提交之前的修改）

```
把刚刚讨论确认的点梳理一下，重新同步回
```



#### 代码到设计文档的反向更新和同步



### 项目结构

前后端项目都放在一个项目里面  or 分离



### 输出接口

#### 前后端分离常规操作

后端项目

```
帮我输出xxxController (或xx功能）的接口文档 到 design/api/xxx.md 文件，我好给前端接口，让他去实现界面
```

前端项目

```
根据 @需求文档.md 和 后端接口文档 @后端导出的接口文档.md 做设计（或者编码）
```

#### 生成 curl命令

````
生成 xxController.yy 方法的 curl 请求例子
````

```
生成 xxHttpClient.yy 方法的curl例子
```



